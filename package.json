{
  "name": "condopay",
  "version": "2.0.0",
  "private": true,
  "description": "CondoPay v2 — Tier 1 (building together)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres"
  }
}
