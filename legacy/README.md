# Legacy code (Firebase MVP)

This folder holds the **previous** CondoPay implementation. It is not used by v2.

| Folder | What it was |
|--------|-------------|
| `frontend-firebase/` | Create React App + Firebase Auth + Firestore |
| `backend-stub/` | Empty Express manifest only (no `index.js`) |

You can delete `legacy/` once you no longer need reference UI or Firestore field names.
